import { type UnidadType } from '../../types/generalTypes'
import { Paper } from '@mui/material'

const UnidadList = ({ unidades }: { unidades: UnidadType[] }) => {
  return (
    <Paper>
      
    </Paper>

  )
}

export default UnidadList